diff --git a/dist/api/anthropic-messages.js b/dist/api/anthropic-messages.js
index 5dd85f19db51b73dcd0c5c4cd8c28ad99e09ee6f..e94fa7784bbe0b8972ec47a9088de6bf52ac4d4c 100644
--- a/dist/api/anthropic-messages.js
+++ b/dist/api/anthropic-messages.js
@@ -507,7 +507,6 @@ export const stream = (model, context, options) => {
                         const block = blocks[index];
                         if (block && block.type === "toolCall") {
                             block.partialJson += event.delta.partial_json;
-                            block.arguments = parseStreamingJson(block.partialJson);
                             stream.push({
                                 type: "toolcall_delta",
                                 contentIndex: index,
diff --git a/dist/api/bedrock-converse-stream.js b/dist/api/bedrock-converse-stream.js
index 44e3f13e808f0665a44e34437e173743f81bd51f..6c4899b29efeec46a67e1b9c1403677c66200a81 100644
--- a/dist/api/bedrock-converse-stream.js
+++ b/dist/api/bedrock-converse-stream.js
@@ -459,7 +459,6 @@ function handleContentBlockDelta(event, blocks, output, stream) {
     }
     else if (delta?.toolUse && block?.type === "toolCall") {
         block.partialJson = (block.partialJson || "") + (delta.toolUse.input || "");
-        block.arguments = parseStreamingJson(block.partialJson);
         stream.push({ type: "toolcall_delta", contentIndex: index, delta: delta.toolUse.input || "", partial: output });
     }
     else if (delta?.reasoningContent) {
diff --git a/dist/api/mistral-conversations.js b/dist/api/mistral-conversations.js
index 3106c5d470dc3610c05d8ee153026df20f86f66e..c54c1b5dc525715c7b9dc4e780cd74b497f0671d 100644
--- a/dist/api/mistral-conversations.js
+++ b/dist/api/mistral-conversations.js
@@ -541,7 +541,6 @@ async function consumeChatStream(model, output, stream, mistralStream) {
                 ? toolCall.function.arguments
                 : JSON.stringify(toolCall.function.arguments || {});
             block.partialArgs = (block.partialArgs || "") + argsDelta;
-            block.arguments = parseStreamingJson(block.partialArgs);
             stream.push({
                 type: "toolcall_delta",
                 contentIndex: toolBlocksByKey.get(key),
diff --git a/dist/api/openai-codex-responses.d.ts b/dist/api/openai-codex-responses.d.ts
index 704476da084be53d6aa194a5f774fa3ad47f5e9a..f06783152e0d2238100faedb348278b8bf7a7ef1 100644
--- a/dist/api/openai-codex-responses.d.ts
+++ b/dist/api/openai-codex-responses.d.ts
@@ -1,5 +1,5 @@
 import type { ResponseCreateParamsStreaming } from "openai/resources/responses/responses.js";
-import type { SimpleStreamOptions, StreamFunction, StreamOptions } from "../types.ts";
+import type { PreparedWebSocket, SimpleStreamOptions, StreamFunction, StreamOptions } from "../types.ts";
 export interface OpenAICodexResponsesOptions extends StreamOptions {
     reasoningEffort?: "none" | "minimal" | "low" | "medium" | "high" | "xhigh" | "max";
     reasoningSummary?: "auto" | "concise" | "detailed" | "off" | "on" | null;
@@ -28,4 +28,6 @@ export interface OpenAICodexWebSocketDebugStats {
 export declare function getOpenAICodexWebSocketDebugStats(sessionId: string): OpenAICodexWebSocketDebugStats | undefined;
 export declare function resetOpenAICodexWebSocketDebugStats(sessionId?: string): void;
 export declare function closeOpenAICodexWebSocketSessions(sessionId?: string): void;
+/** Retire one connector and close only its cached sockets and fallback partitions. The owner closes pending sockets. */
+export declare function closeOpenAICodexWebSocketConnector(connector: PreparedWebSocket["connect"]): void;
 //# sourceMappingURL=openai-codex-responses.d.ts.map
diff --git a/dist/api/openai-codex-responses.js b/dist/api/openai-codex-responses.js
index dfdb6ec3651febbba6b8788f16118ecbf4b38fa8..5e62456be090a8236024b3fff74d0c193c9c1d99 100644
--- a/dist/api/openai-codex-responses.js
+++ b/dist/api/openai-codex-responses.js
@@ -182,10 +182,22 @@ export const stream = (model, context, options) => {
             const httpTimeoutMs = normalizeTimeoutMs(options?.timeoutMs);
             const websocketConnectTimeoutMs = normalizeTimeoutMs(options?.websocketConnectTimeoutMs);
             const transport = options?.transport || "auto";
+            // Prepare before fallback/cache lookup so every request validates its current authentication.
+            const preparedWebSocket = transport !== "sse" && options?.websocketFactory
+                ? options.websocketFactory(resolveCodexWebSocketUrl(model.baseUrl), headersToRecord(websocketHeaders), options.env)
+                : undefined;
+            const connectionPartition = preparedWebSocket
+                ? await websocketPartition(preparedWebSocket)
+                : undefined;
+            const connectionAccount = connectionPartition ? accountId + ":" + connectionPartition : accountId;
+            const fallbackSessionId = connectionPartition && cacheSessionId
+                ? cacheSessionId + "\0" + connectionPartition
+                : cacheSessionId;
+            assertWebSocketConnectorActive(preparedWebSocket);
             let startEmitted = false;
-            const websocketDisabledForSession = transport !== "sse" && isWebSocketSseFallbackActive(cacheSessionId);
+            const websocketDisabledForSession = transport !== "sse" && isWebSocketSseFallbackActive(fallbackSessionId);
             if (websocketDisabledForSession) {
-                recordWebSocketSseFallback(cacheSessionId);
+                recordWebSocketSseFallback(cacheSessionId, fallbackSessionId);
             }
             if (transport !== "sse" && !websocketDisabledForSession) {
                 let websocketStarted = false;
@@ -200,7 +212,7 @@ export const stream = (model, context, options) => {
                                 startEmitted = true;
                                 stream.push({ type: "start", partial: output });
                             }
-                        }, httpTimeoutMs, websocketConnectTimeoutMs, cacheSessionId, accountId, grammarToolInputProperties, options);
+                        }, httpTimeoutMs, websocketConnectTimeoutMs, cacheSessionId, connectionAccount, grammarToolInputProperties, options, preparedWebSocket);
                         if (options?.signal?.aborted) {
                             throw new Error("Request was aborted");
                         }
@@ -214,6 +226,7 @@ export const stream = (model, context, options) => {
                         return;
                     }
                     catch (error) {
+                        assertWebSocketConnectorActive(preparedWebSocket);
                         const aborted = options?.signal?.aborted;
                         const connectionLimitBeforeStart = !websocketStarted && isWebSocketConnectionLimitReachedError(error);
                         const previousResponseNotFound = isPreviousResponseNotFoundError(error);
@@ -235,11 +248,12 @@ export const stream = (model, context, options) => {
                             phase: websocketStarted ? "after_message_stream_start" : "before_message_stream_start",
                             requestBytes: new TextEncoder().encode(bodyJson).byteLength,
                         }));
-                        recordWebSocketFailure(cacheSessionId, error);
+                        trackWebSocketConnectorPartition(preparedWebSocket, cacheSessionId, connectionAccount);
+                        recordWebSocketFailure(cacheSessionId, error, fallbackSessionId);
                         if (websocketStarted) {
                             throw error;
                         }
-                        recordWebSocketSseFallback(cacheSessionId);
+                        recordWebSocketSseFallback(cacheSessionId, fallbackSessionId);
                         break;
                     }
                 }
@@ -257,6 +271,7 @@ export const stream = (model, context, options) => {
             let lastError;
             const maxRetries = options?.maxRetries ?? DEFAULT_MAX_RETRIES;
             for (let attempt = 0; attempt <= maxRetries; attempt++) {
+                assertWebSocketConnectorActive(preparedWebSocket);
                 if (options?.signal?.aborted) {
                     throw new Error("Request was aborted");
                 }
@@ -662,12 +677,25 @@ export function resetOpenAICodexWebSocketDebugStats(sessionId) {
     if (sessionId) {
         websocketDebugStats.delete(sessionId);
         websocketSseFallbackSessions.delete(sessionId);
+        for (const key of websocketSseFallbackSessions) {
+            if (key.startsWith(sessionId + "\0")) websocketSseFallbackSessions.delete(key);
+        }
         return;
     }
     websocketDebugStats.clear();
     websocketSseFallbackSessions.clear();
 }
 export function closeOpenAICodexWebSocketSessions(sessionId) {
+    for (const [connector, partitions] of websocketConnectorPartitions) {
+        for (const [key, entry] of partitions) {
+            if (!sessionId || entry.sessionId === sessionId) partitions.delete(key);
+        }
+        if (partitions.size === 0) websocketConnectorPartitions.delete(connector);
+    }
+    for (const key of websocketSseFallbackSessions) {
+        if (sessionId ? key.startsWith(sessionId + "\0") : key.includes("\0"))
+            websocketSseFallbackSessions.delete(key);
+    }
     const closeEntry = (entry) => {
         if (entry.idleTimer)
             clearTimeout(entry.idleTimer);
@@ -686,25 +714,79 @@ export function closeOpenAICodexWebSocketSessions(sessionId) {
     websocketSessionCache.clear();
 }
 registerSessionResourceCleanup(closeOpenAICodexWebSocketSessions);
+const websocketConnectorPartitions = new Map();
+const disposedWebSocketConnectors = new WeakSet();
+function trackWebSocketConnectorPartition(prepared, sessionId, accountId) {
+    assertWebSocketConnectorActive(prepared);
+    if (!prepared || !sessionId) return;
+    let partitions = websocketConnectorPartitions.get(prepared.connect);
+    if (!partitions) {
+        partitions = new Map();
+        websocketConnectorPartitions.set(prepared.connect, partitions);
+    }
+    const partition = accountId.slice(accountId.lastIndexOf(":") + 1);
+    partitions.set(sessionId + "\0" + partition, { sessionId, accountId });
+}
+function assertWebSocketConnectorActive(prepared, socket) {
+    if (prepared && disposedWebSocketConnectors.has(prepared.connect)) {
+        if (socket) closeWebSocketSilently(socket, 1000, "connector_disposed");
+        throw new Error("WebSocket connector is disposed");
+    }
+}
+export function closeOpenAICodexWebSocketConnector(connector) {
+    disposedWebSocketConnectors.add(connector);
+    const sessions = new Set();
+    for (const [key, { sessionId, accountId }] of websocketConnectorPartitions.get(connector) ?? []) {
+        sessions.add(sessionId);
+        websocketSseFallbackSessions.delete(key);
+        const entries = websocketSessionCache.get(sessionId);
+        const entry = entries?.get(accountId);
+        if (entry) {
+            if (entry.idleTimer) clearTimeout(entry.idleTimer);
+            closeWebSocketSilently(entry.socket, 1000, "connector_disposed");
+            entries.delete(accountId);
+            if (entries.size === 0) websocketSessionCache.delete(sessionId);
+        }
+    }
+    websocketConnectorPartitions.delete(connector);
+    for (const sessionId of sessions) {
+        const stats = websocketDebugStats.get(sessionId);
+        if (stats) stats.websocketFallbackActive = [...websocketSseFallbackSessions].some(key => key === sessionId || key.startsWith(sessionId + "\0"));
+    }
+}
 function isWebSocketSseFallbackActive(sessionId) {
     return sessionId ? websocketSseFallbackSessions.has(sessionId) : false;
 }
-function recordWebSocketSseFallback(sessionId) {
+function recordWebSocketSseFallback(sessionId, fallbackSessionId = sessionId) {
     if (!sessionId)
         return;
     const stats = getOrCreateWebSocketDebugStats(sessionId);
     stats.sseFallbacks++;
-    stats.websocketFallbackActive = isWebSocketSseFallbackActive(sessionId);
+    stats.websocketFallbackActive = isWebSocketSseFallbackActive(fallbackSessionId);
 }
-function recordWebSocketFailure(sessionId, error) {
+function recordWebSocketFailure(sessionId, error, fallbackSessionId = sessionId) {
     if (!sessionId)
         return;
-    websocketSseFallbackSessions.add(sessionId);
+    websocketSseFallbackSessions.add(fallbackSessionId);
     const stats = getOrCreateWebSocketDebugStats(sessionId);
     stats.websocketFailures++;
     stats.lastWebSocketError = formatThrownValue(error);
     stats.websocketFallbackActive = true;
 }
+const websocketConnectorIds = new WeakMap();
+let nextWebsocketConnectorId = 0;
+async function websocketPartition(prepared) {
+    assertWebSocketConnectorActive(prepared);
+    let connectorId = websocketConnectorIds.get(prepared.connect);
+    if (connectorId === undefined) {
+        connectorId = ++nextWebsocketConnectorId;
+        websocketConnectorIds.set(prepared.connect, connectorId);
+    }
+    const headers = [...new Headers(prepared.headers).entries()].sort(([a], [b]) => a.localeCompare(b));
+    const bytes = new TextEncoder().encode(JSON.stringify([prepared.url, headers, connectorId, prepared.proxyUrl]));
+    const digest = await crypto.subtle.digest("SHA-256", bytes);
+    return Array.from(new Uint8Array(digest), byte => byte.toString(16).padStart(2, "0")).join("");
+}
 let _cachedWebsocket = null;
 async function getWebSocketConstructor(env) {
     if (!env && _cachedWebsocket)
@@ -780,9 +862,11 @@ function scheduleSessionWebSocketExpiry(sessionId, accountId, entry) {
             websocketSessionCache.delete(sessionId);
     }, SESSION_WEBSOCKET_CACHE_TTL_MS);
 }
-async function connectWebSocket(url, headers, signal, connectTimeoutMs = DEFAULT_WEBSOCKET_CONNECT_TIMEOUT_MS, env) {
-    const WebSocketCtor = await getWebSocketConstructor(env);
-    if (!WebSocketCtor) {
+async function connectWebSocket(url, headers, signal, connectTimeoutMs = DEFAULT_WEBSOCKET_CONNECT_TIMEOUT_MS, env, prepared) {
+    assertWebSocketConnectorActive(prepared);
+    if (prepared) signal?.throwIfAborted();
+    const WebSocketCtor = prepared ? undefined : await getWebSocketConstructor(env);
+    if (!prepared && !WebSocketCtor) {
         throw new Error("WebSocket transport is not available in this runtime");
     }
     const wsHeaders = headersToRecord(headers);
@@ -792,7 +876,8 @@ async function connectWebSocket(url, headers, signal, connectTimeoutMs = DEFAULT
         let timeout;
         let socket;
         try {
-            socket = new WebSocketCtor(url, { headers: wsHeaders });
+            if (prepared) signal?.throwIfAborted();
+            socket = prepared ? prepared.connect(prepared.url, prepared.headers, prepared.proxyUrl) : new WebSocketCtor(url, { headers: wsHeaders });
         }
         catch (error) {
             reject(error instanceof Error ? error : new Error(String(error)));
@@ -821,6 +906,10 @@ async function connectWebSocket(url, headers, signal, connectTimeoutMs = DEFAULT
         const onOpen = () => {
             if (settled)
                 return;
+            if (prepared && disposedWebSocketConnectors.has(prepared.connect)) {
+                fail(new Error("WebSocket connector is disposed"), "connector_disposed");
+                return;
+            }
             settled = true;
             cleanup();
             resolve(socket);
@@ -848,9 +937,10 @@ async function connectWebSocket(url, headers, signal, connectTimeoutMs = DEFAULT
         }
     });
 }
-async function acquireWebSocket(url, headers, sessionId, accountId, signal, connectTimeoutMs, env) {
+async function acquireWebSocket(url, headers, sessionId, accountId, signal, connectTimeoutMs, env, prepared) {
+    assertWebSocketConnectorActive(prepared);
     if (!sessionId) {
-        const socket = await connectWebSocket(url, headers, signal, connectTimeoutMs, env);
+        const socket = await connectWebSocket(url, headers, signal, connectTimeoutMs, env, prepared);
         return {
             socket,
             reused: false,
@@ -877,7 +967,7 @@ async function acquireWebSocket(url, headers, sessionId, accountId, signal, conn
                 entry: cached,
                 reused: true,
                 release: ({ keep } = {}) => {
-                    if (!keep || !isWebSocketReusable(cached.socket)) {
+                    if (!keep || !isWebSocketReusable(cached.socket) || (prepared && (disposedWebSocketConnectors.has(prepared.connect) || websocketSessionCache.get(sessionId)?.get(accountId) !== cached))) {
                         closeWebSocketSilently(cached.socket);
                         const currentEntries = websocketSessionCache.get(sessionId);
                         if (currentEntries?.get(accountId) === cached)
@@ -892,7 +982,7 @@ async function acquireWebSocket(url, headers, sessionId, accountId, signal, conn
             };
         }
         if (cached.busy) {
-            const socket = await connectWebSocket(url, headers, signal, connectTimeoutMs, env);
+            const socket = await connectWebSocket(url, headers, signal, connectTimeoutMs, env, prepared);
             return {
                 socket,
                 reused: false,
@@ -908,7 +998,9 @@ async function acquireWebSocket(url, headers, sessionId, accountId, signal, conn
                 websocketSessionCache.delete(sessionId);
         }
     }
-    const socket = await connectWebSocket(url, headers, signal, connectTimeoutMs, env);
+    const socket = await connectWebSocket(url, headers, signal, connectTimeoutMs, env, prepared);
+    assertWebSocketConnectorActive(prepared, socket);
+    trackWebSocketConnectorPartition(prepared, sessionId, accountId);
     const entry = { socket, busy: true, createdAt: Date.now() };
     accountEntries = websocketSessionCache.get(sessionId);
     if (!accountEntries) {
@@ -921,7 +1013,7 @@ async function acquireWebSocket(url, headers, sessionId, accountId, signal, conn
         entry,
         reused: false,
         release: ({ keep } = {}) => {
-            if (!keep || !isWebSocketReusable(entry.socket)) {
+            if (!keep || !isWebSocketReusable(entry.socket) || (prepared && (disposedWebSocketConnectors.has(prepared.connect) || websocketSessionCache.get(sessionId)?.get(accountId) !== entry))) {
                 closeWebSocketSilently(entry.socket);
                 if (entry.idleTimer)
                     clearTimeout(entry.idleTimer);
@@ -1153,10 +1245,11 @@ async function* startWebSocketOutputOnFirstEvent(events, onStart) {
         yield event;
     }
 }
-async function processWebSocketStream(url, body, headers, output, stream, model, onStart, idleTimeoutMs, websocketConnectTimeoutMs, cacheSessionId, accountId, grammarToolInputProperties, options) {
-    const { socket, entry, reused, release } = await acquireWebSocket(url, headers, cacheSessionId, accountId, options?.signal, websocketConnectTimeoutMs, options?.env);
+async function processWebSocketStream(url, body, headers, output, stream, model, onStart, idleTimeoutMs, websocketConnectTimeoutMs, cacheSessionId, accountId, grammarToolInputProperties, options, preparedWebSocket) {
+    const { socket, entry, reused, release } = await acquireWebSocket(url, headers, cacheSessionId, accountId, options?.signal, websocketConnectTimeoutMs, options?.env, preparedWebSocket);
     let keepConnection = true;
-    const useCachedContext = options?.transport === "websocket-cached" || options?.transport === "auto";
+    assertWebSocketConnectorActive(preparedWebSocket, socket);
+    const useCachedContext = options?.transport === "websocket-cached" || options?.transport === "auto" || (preparedWebSocket !== undefined && options?.transport === undefined);
     // ChatGPT Codex Responses rejects `store: true` ("Store must be set to false").
     // WebSocket continuation still works via connection-scoped previous_response_id state.
     const fullBody = body;
diff --git a/dist/api/openai-completions.js b/dist/api/openai-completions.js
index fc8e89382b75f259eb4f041b522840b0d603c033..f5acd00ff7c0e4db054abfcd1b9e5ac4a6005959 100644
--- a/dist/api/openai-completions.js
+++ b/dist/api/openai-completions.js
@@ -437,7 +437,6 @@ export const stream = (model, context, options) => {
                             if (toolCall.function?.arguments) {
                                 delta = toolCall.function.arguments;
                                 block.partialArgs = (block.partialArgs ?? "") + toolCall.function.arguments;
-                                block.arguments = parseStreamingJson(block.partialArgs);
                             }
                             else if (toolCall.custom?.input) {
                                 const nextInput = getCustomToolCallInput(block) + toolCall.custom.input;
diff --git a/dist/api/openai-responses-shared.js b/dist/api/openai-responses-shared.js
index 53597d2c3becdd2c03d4fd265c213a36b403f26b..0aa5752a3e4bad3c0c5aa04d0405448e0fa488be 100644
--- a/dist/api/openai-responses-shared.js
+++ b/dist/api/openai-responses-shared.js
@@ -546,7 +546,6 @@ export async function processResponsesStream(openaiStream, output, stream, model
             if (!slot || slot.block.partialJson === undefined)
                 continue;
             slot.block.partialJson += event.delta;
-            slot.block.arguments = parseStreamingJson(slot.block.partialJson);
             pushToolCallDelta(slot, event.delta);
         }
         else if (event.type === "response.function_call_arguments.done") {
diff --git a/dist/api/pi-messages.js b/dist/api/pi-messages.js
index ad5f4605e8e157f12b8948e88e0c66e8a5494fc4..5609d61b49264b26fc86a787ec19780d29cdfcab 100644
--- a/dist/api/pi-messages.js
+++ b/dist/api/pi-messages.js
@@ -155,8 +155,6 @@ function createEventConverter(model) {
             case "toolcall_delta": {
                 const json = `${toolJson.get(event.contentIndex) ?? ""}${event.delta}`;
                 toolJson.set(event.contentIndex, json);
-                partial.content[event.contentIndex].arguments =
-                    parseStreamingJson(json);
                 break;
             }
             case "toolcall_end":
diff --git a/dist/api/simple-options.js b/dist/api/simple-options.js
index 216a45feebc02acef047d1ff133729fb0751b76c..57bc8170dd89aad24721d1ee992acd579a0d2a79 100644
--- a/dist/api/simple-options.js
+++ b/dist/api/simple-options.js
@@ -19,6 +19,7 @@ export function buildBaseOptions(model, context, options, apiKey) {
         telemetryContext: options?.telemetryContext,
         apiKey: apiKey || options?.apiKey,
         fetch: options?.fetch,
+        websocketFactory: options?.websocketFactory,
         transport: options?.transport,
         cacheRetention: options?.cacheRetention,
         sessionId: options?.sessionId,
diff --git a/dist/types.d.ts b/dist/types.d.ts
index 5915ca64b161bc7bcebdb14d2ad1a2c18cde942e..46aacbc0470b393bd50ab8ea677a7c11ae3d999f 100644
--- a/dist/types.d.ts
+++ b/dist/types.d.ts
@@ -48,6 +48,19 @@ export type Transport = "sse" | "websocket" | "websocket-cached" | "auto";
 export type ProviderEnv = Record<string, string>;
 export type ProviderHeaders = Record<string, string | null>;
 export type FetchFunction = typeof globalThis.fetch;
+/** A socket implementing the event API consumed by the Codex transport. */
+export type ProviderWebSocket = Pick<WebSocket, "readyState" | "send" | "close" | "addEventListener" | "removeEventListener">;
+/** Final authenticated handshake, prepared before connection reuse is considered. */
+export interface PreparedWebSocket {
+    url: string;
+    headers: Record<string, string>;
+    /** Resolved proxy for this handshake; included in the connection reuse partition. */
+    proxyUrl?: string;
+    /** Keep this function stable across requests to permit connection reuse. */
+    connect: (url: string, headers: Readonly<Record<string, string>>, proxyUrl?: string) => ProviderWebSocket;
+}
+/** Synchronous per-request handshake preparation; throws before fallback or socket dispatch. */
+export type WebSocketFactory = (url: string, headers: Readonly<Record<string, string>>, env?: ProviderEnv) => PreparedWebSocket;
 export type SessionAffinityFormat = "openai" | "openai-nosession" | "openrouter";
 export interface ProviderResponse {
     status: number;
@@ -65,6 +78,8 @@ export interface ProviderRequestOptions<TModel = Model<Api>> {
      * This does not affect WebSocket transports.
      */
     fetch?: FetchFunction;
+    /** Optional Codex WebSocket handshake factory; omission preserves the runtime constructor path. */
+    websocketFactory?: WebSocketFactory;
     /**
      * Provider-scoped environment values. These take precedence over process.env for
      * provider configuration such as regional settings, endpoint placeholders, and
