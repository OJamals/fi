/**
 * @fi/authorization-bundle — the authorization seam as a profile bundle layer.
 * The package's substance is `cordis.patch.yml`, declared by the
 * `dsh.bundle.patch` manifest field and resolved by the profile composer
 * through that field; this module carries no runtime API.
 * @module @fi/authorization-bundle
 */

export {}
